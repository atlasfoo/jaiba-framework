# JAIBA — local-only brain state. Do not commit.
#
# Copied to `.ai/.gitignore` by jaiba-scaffold.
#   session/        short-term workspace: plan.md, tasks.md, walkthrough.md.
#                   Per-developer, per-task scratch — never shared.
session/
