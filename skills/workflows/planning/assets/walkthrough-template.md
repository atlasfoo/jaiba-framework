# Walkthrough: <plan slug>

> Bitácora narrativa de la ejecución del plan. Append one block per
> completed phase. Don't log task-by-task — aggregate at the phase
> boundary.
>
> When the developer reads this later, they should be able to
> reconstruct *what happened and why* without re-reading the code.

---

<!-- Append phase blocks below this line as they complete. Format: -->

<!--
## Phase <N> — <Theme>     <YYYY-MM-DD>

**Outcome.** <3–6 lines on what was actually done.>

**Decisions.**
- <Decision 1 and the reasoning that led to it.>
- <Decision 2.>

**Deviations from plan.**
- <Any tasks added, removed, or reworded during this phase, with the
  reason. If the plan itself was amended, note it here and check that
  `plan.md`'s "Plan amendments" section reflects it.>

**ADR candidates.**
- <Anything structural enough that `summarize` should consider it
  for `adr-log.md`. If nothing, write "none".>

**Quality gate.** Pass | Fail-then-fixed | <details>
-->

<!--
Out-of-band change block (written by the `fast` skill when it makes an
unplanned adjustment to this plan). Use this instead of a phase block:

## Out-of-band change (fast)     <YYYY-MM-DD>

**Change.** <1–2 lines on what was adjusted and why it wasn't in the
plan.>

**Scope touched.** <files / tasks affected; the phase it relates to>

**Quality gate.** Pass | Fail-then-fixed | <details>
-->
